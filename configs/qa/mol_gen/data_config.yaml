# Data Configuration for Molecule Generation/Captioning

# Data paths
root: data/Molecule-oriented_Instructions/mol_gen_full_v2/

# Task type: 'qa' (multiple choice), 'caption' (captioning/generation), or 'property' (regression)
task_type: caption

# Molecular representation type
# Options: 'mol', 'SMILES', 'SMILES,mol', 'SELFIES', 'SMILES,<graph>'
mol_type: SMILES,mol

# Legacy fields (kept for compatibility, not used in new HF approach)
num_workers: 16
batch_size: 4
